[build]
builder = "nixpacks"
buildCommand = "npm install"  # Next.js 빌드 건너뛰기 (Socket.io만 필요)

[deploy]
startCommand = "npm run socket:start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
